#include <stdio.h>

int main() {
    printf("Hello, world!\n");
    return 0;
}

/*
1. Ensure you have a C compiler (e.g. gcc).
   Check with:
       gcc --version

2. Compile the file:
       gcc hello.c -o hello

3. Run the output:
       ./hello

You should see:
    Hello, world!
*/
